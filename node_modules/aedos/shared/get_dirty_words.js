const client = require("../config/db_config");
module.exports = async function (languages) {
  let list = [];

  for (let { lang } of languages) {
    const { rows } = await client.query(
      `SELECT * FROM dirty_words where language='${lang}'`
    );
    list.push(rows);
  }

  return list.flat(1);
};
